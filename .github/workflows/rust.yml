#                \||/
#                |  @___oo
#      /\  /\   / (__,,,,| (Deployment)
#     ) /^\) ^\/ _)
#     )   /^\/   _)
#     )   _ /  / _)
# /\  )/\/ ||  | )_)
#<  >      |(,,) )__)
# ||      /    \)___)\
# | \____(      )___) )___
#  \______(_______;;; __;;;

name: Fabled-Engine

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
      - name: Build
        run: cargo build --all
      - name: Run tests
        run: cargo test --all
